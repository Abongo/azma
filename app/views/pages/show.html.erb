p id="gallery"><%= gallery %></p>

<p>
	<strong>Title:</strong>
	<%= @gallery.title %>
</p>

<p>
	<strong>Body:</strong>
	<%= @gallery.body %>
</p>
 
<%= link_to 'Edit', edit_page_gallery_path(@gallery) %> |
<%= link_to 'Back', galleries_path %>
